---
vaccine: loop-exists
generation: "202608301704"
dose: every-commit
---
Disease
The perpetual loop is quietly retired, so the repo has a ledger but nothing that advances it.

Symptom
gridatlas scope 6 removed the schedule; scope-loop now runs only on manual dispatch.

Antibody
```js
export default ({ workflows, scopes }) => {
  if (!scopes.length) return [];
  const loops = workflows.filter(w => /scope-loop/.test(w.file));
  if (!loops.length) return ["scope-of-works exists but no scope-loop workflow"];
  return loops.filter(w => !/schedule:/.test(w.text)).map(w => `${w.file} has no schedule; the loop is not perpetual`);
};
```

Dose
Runs every-commit.

Provenance
gridatlas replay study 202608301700-cs-study-scope.md, finding FN4.
