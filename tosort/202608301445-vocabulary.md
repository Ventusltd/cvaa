---
vaccine: vocabulary
generation: "202608301445"
dose: every-loop
---
Disease
Words drift between sessions: one file says done, another says complete, a model invents closed, and the loop can no longer parse state.

Symptom
gridatlas scope files define active, done, blocked; nothing prevented a fourth value.

Antibody
```js
export default ({ scopes, registry }) => {
  const STATUS = new Set(["active", "done", "blocked"]);
  const DOSE = new Set(["every-loop", "every-deploy", "every-commit"]);
  const out = [];
  for (const s of scopes) if (s.status && !STATUS.has(s.status)) out.push(`${s.file}: status "${s.status}" is not in the vocabulary (active|done|blocked)`);
  for (const v of registry) if (!DOSE.has(v.dose)) out.push(`${v.file}: dose "${v.dose}" is not in the vocabulary`);
  return out;
};
```

Dose
Runs every-loop.

Provenance
Vocabulary frozen 2026-08-30. generation: 12-digit timestamp, the only ordering. scope of works: linked list of units of work, one active. cartridge: hash-pinned module a release loads. pointer: the one file naming what is live. vaccine: a failure mode plus its executable antibody. disease, symptom, antibody, dose, provenance: the five sections. inoculate: run the registry against a repo. immune: zero findings. finding: one failure string. superseded_by: retire without deleting. baseline: grandfathered count in cvaa.json that can only fall. STATE: the generated page read first. hostile amnesia: the enemy.
